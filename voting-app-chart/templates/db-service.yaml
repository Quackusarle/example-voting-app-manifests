apiVersion: v1
kind: Service
metadata:
  labels:
    app: db
  name: db
spec:
  type: {{ .Values.db.service.type }}
  ports:
  - name: "db-service"
    port: {{ .Values.db.service.port }}
    targetPort: {{ .Values.db.service.port }}
  selector:
    app: db